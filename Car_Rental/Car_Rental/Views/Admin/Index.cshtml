@model IEnumerable<Car_Rental.Models.Admin>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Авторизация и Регистрация</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            margin: 0;
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .form {
            display: flex;
            flex-direction: column;
        }

            .form input {
                margin-bottom: 10px;
                padding: 10px;
                border: 1px solid #ccc;
                border-radius: 3px;
            }

            .form button {
                padding: 10px;
                border: none;
                border-radius: 3px;
                background-color: #5cb85c;
                color: white;
                font-size: 16px;
                cursor: pointer;
            }

                .form button:hover {
                    background-color: #4cae4c;
                }

        .toggle-link {
            text-align: center;
            margin-top: 10px;
            color: #007bff;
            cursor: pointer;
        }

            .toggle-link:hover {
                text-decoration: underline;
            }
    </style>
</head>
<body>
    <div class="container">
        <div id="auth-container">
            <!-- Форма авторизации -->
            <div id="login-form" class="form">
                <h2>Авторизация</h2>
                <input type="email" id="login-email" placeholder="Email" required>
                <input type="password" id="login-password" placeholder="Пароль" required>
                <button type="button" onclick="window.location.href='@Url.Action("Login", "Admin")'">Войти</button>
                <div class="toggle-link" onclick="toggleForm()">Нет аккаунта? Зарегистрируйтесь</div>
            </div>
            <!-- Форма регистрации -->
            <div id="register-form" class="form" style="display: none;">
                <h2>Регистрация</h2>
                <input type="text" id="register-username" placeholder="Имя пользователя" required>
                <input type="email" id="register-email" placeholder="Email" required>
                <input type="password" id="register-password" placeholder="Пароль" required>
                <button type="button" onclick="window.location.href='@Url.Action("Register", "Admin")'">Зарегистрироваться</button>
                <div class="toggle-link" onclick="toggleForm()">Уже есть аккаунт? Войдите</div>
            </div>
        </div>
    </div>
    <script>
        function toggleForm() {
            const loginForm = document.getElementById('login-form');
            const registerForm = document.getElementById('register-form');
            if (loginForm.style.display === 'none') {
                loginForm.style.display = 'flex';
                registerForm.style.display = 'none';
            } else {
                loginForm.style.display = 'none';
                registerForm.style.display = 'flex';
            }
        }

        function login() {
            const email = document.getElementById('login-email').value;
            const password = document.getElementById('login-password').value;
            // Добавьте сюда логику авторизации
<<<<<<< HEAD
            
=======
            //window.location.href = '/Views/Car/Index.cshtml';
>>>>>>> 181534acc8d5d9bee18a9edf30ccd069b9985731
            console.log(`Вход: ${email}, ${password}`);
            
                // $.ajax({
                //     url: '@Url.Action("Login", "Admin")',
                //     type: 'GET',
                //     data: { String: email, String password },
                //     success: function (email, password) {
                //         alert('ID: ' + data.id + '\nEmail: ' + data.email + '\nUsername: ' + data.username);
                //     },
                //     error: function () {
                //         alert('Ошибка при получении данных администратора.');
                //     }
                // });
            
        }

        function register() {
            const username = document.getElementById('register-username').value;
            const email = document.getElementById('register-email').value;
            const password = document.getElementById('register-password').value;
            // Добавьте сюда логику регистрации
            console.log(`Регистрация: ${username}, ${email}, ${password}`);
        }
    </script>
</body>
</html>




@*<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreateLogin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatePassword)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sales)
            </th>
            <th></th>
        </tr>
    </thead>
 <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreateLogin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatePassword)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sales)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
    
</table>
</body>
</html>
*@
