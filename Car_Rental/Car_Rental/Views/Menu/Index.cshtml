<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Выбор функции</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            margin: 0;
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .option {
            padding: 10px;
            margin: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            font-size: 18px;
            text-decoration: none;
            display: block;
        }

            .option:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="container">
        <h2>Выберите функцию</h2>
        <a href="@Url.Action("Index", "Customers")" class="option">База данных пользователей</a>
        <a href="@Url.Action("Index", "Car")" class="option">База данных автомобилей</a>
        <a href="@Url.Action("Index", "Rent")" class="option">База данных аренд</a>
        <a href="@Url.Action("Index", "Discount")" class="option">База данных скидок</a>
        <a href="@Url.Action("Index", "ServiceDate")" class="option">База данных о сервисном обслуживании</a>
        <a href="@Url.Action("Index", "Card")" class="option">База данных дисконтных карт</a>
        <a href="@Url.Action("Index", "Contract")" class="option">База данных контрактов</a>
        <a href="@Url.Action("Index", "Order")" class="option">Создать заказ</a>
    </div>

    
</body>
</html>