<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Выбор функции</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            margin: 0;
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .option {
            padding: 10px;
            margin: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            font-size: 18px;
            text-decoration: none;
            display: block;
        }

            .option:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="container">
        <h2>Выберите функцию</h2>
        <a href="#users" class="option">База данных пользователей</a>
        <a href="#cars" class="option">База данных автомобилей</a>
        <a href="#rentals" class="option">База данных аренд</a>
        <a href="#discounts" class="option">База данных скидок</a>
        <a href="#service" class="option">База данных о сервисном обслуживании</a>
        <a href="#loyalty-discounts" class="option">База данных дисконтных скидок</a>
        <a href="#contacts" class="option">Контакты</a>
    </div>

    <script>
        // Здесь можно добавить логику для перехода по ссылкам или открытия соответствующих разделов
        document.querySelectorAll('.option').forEach(option => {
            option.addEventListener('click', function (event) {
                event.preventDefault();
                alert(`Вы выбрали: ${this.textContent}`);
                // Здесь можно добавить логику для перехода на соответствующую страницу или раздел
                // Например, window.location.href = 'user_database.html';
            });
        });
    </script>
</body>
</html>